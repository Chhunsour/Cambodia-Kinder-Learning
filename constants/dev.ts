/**
 * Temporary Development Configuration
 * Centralized flags for navigation prototyping prior to SQLite profile persistence.
 * Toggle ONBOARDING_COMPLETED to test routing to /(onboarding) vs /(main)/home.
 */
export const DEV_CONFIG = {
  ONBOARDING_COMPLETED: false,
};
